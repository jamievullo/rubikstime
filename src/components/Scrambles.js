import React from 'react'

class Scrambles extends React.Component {

    scramblingAlgo(){
        // const options =[F, R, U, B, L, D, F2, R2, U2, B2, L2, D2, F1, R1, U1, B1, L1, D1]
    }

    render() {
        return (
            <div>
                F, F2, F', R, R2, R', U, U2, U', B, B2, B', L, L2, L', D, D2, D', B, B2
                
            </div>
        )
    }
}

export default Scrambles

